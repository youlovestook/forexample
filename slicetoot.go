package main

import "fmt"

func main() {

	// COMPOSITE LITERAL format
	// we have the type followed by the values
	// x:= type{values}
	// slice allows you to group together values of the same type
	x := []int{4, 5, 7, 8, 42}
	fmt.Println(x)

}
